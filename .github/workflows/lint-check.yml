name: lint-check

on: push

jobs:
  lint-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: |
          make install

      - name: run linter
        run: |
          make lint
